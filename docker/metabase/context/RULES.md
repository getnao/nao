# Ecommerce fixture

Revenue is `order_items.quantity * order_items.unit_price`.

Completed revenue includes only orders whose status is `completed`. Pending and refunded orders are excluded.

The fixture reporting period is January through December 2025.
