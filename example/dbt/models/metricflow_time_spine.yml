version: 2

models:
  - name: metricflow_time_spine
    description: Daily calendar MetricFlow joins against for time-based and cumulative metrics.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
