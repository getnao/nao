name: jaffle_shop
version: 1.0.0
profile: jaffle_shop

model-paths: ["models"]
seed-paths: ["seeds"]

clean-targets:
  - target
  - dbt_packages

models:
  jaffle_shop:
    +materialized: table
    staging:
      +materialized: view
